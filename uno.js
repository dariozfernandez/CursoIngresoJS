
function Mostrar()
{
		//pedir la altura de un triangulo equilatero. informar el perimetro y la superficie

		var laBase= 0;
		var altura= 0;
		var perimetro= 0;
		var superficie= 0 ;


		laBase=document.getElementById('laBase').value;
		laBase=parseInt(laBase);

		altura=prompt('ingrese la altura').value;
		altura=parseInt(altura);

		superficie= (laBase * altura)/2;









}
